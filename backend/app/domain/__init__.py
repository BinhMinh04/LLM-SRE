"""Domain layer: pure product concepts and rules.

Depends on nothing project-external except tiny pure utilities — no framework, DB, provider, or
process/env (see docs/ARCHITECTURE.md dependency rule).
"""
